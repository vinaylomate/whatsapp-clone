apiVersion: v1
kind: Service
metadata: { name: api-gateway, namespace: whatsapp }
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector: { app: api-gateway }
